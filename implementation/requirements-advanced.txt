# Core RAG Agent Dependencies
pydantic-ai>=0.0.14
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pgvector>=0.3.5
python-dotenv>=1.0.0
transformers>=4.40.0
torch>=2.0.0
faiss-cpu>=1.8.1

# LLM and Embeddings
openai>=1.54.0

# Document Processing (Docling)
docling>=2.11.1
transformers>=4.40.0
torch>=2.0.0

# Re-ranking
sentence-transformers>=2.2.2

# For BM25 search
rank-bm25>=0.2.2
numpy>=1.21.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0
factory-boy>=3.3.0
faker>=19.3.0
streamlit

# Optional extras (OCR / ASR / Image processing)
# Install these if you want Docling to support scanned PDFs, OCR engines, or audio transcription
onnxruntime>=1.15.0        # used by RapidOCR and ONNX-based OCR models
easyocr>=1.5.0             # optional OCR engine
# tesserocr requires system tesseract (apt: tesseract-ocr); include only if system dependency is present
tesserocr>=2.5.3
openai-whisper>=20250625   # Whisper ASR for audio transcription (or use `uv sync --extra asr`)
opencv-python-headless>=4.7.0  # Image processing without GUI dependencies
ffmpeg-python>=0.2.0      # Audio and video processing
pydub>=0.25.1             # Audio processing
librosa>=0.10.0           # Audio analysis
psutil>=6.1.0             # System resource monitoring for adaptive ingestion
